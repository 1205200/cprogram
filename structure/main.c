#include <stdio.h>
#include <stdlib.h>
int main()
{
  struct student ;
{
  int rno;
  char name[20];
  int m1,m2,m3;
  int total;
  float average;
  char grade;
}int s[5];
int i;
for(i=0;i<5;i++)
    s[i].rno=i+1;
    scanf("%d %d",&s[i].rno,&s[i].rno);
    scanf("%s %s",s[i].name);
    scanf("%d %d %d %d %d %d",&s[i].m1,&s[i].m2,&s[i].m3,&s[i].m1,&s[i].m2,&s[i].m3);
s[i].total=s[i].m1+s[i].m2+s[i].m3;
s[i].average=s[i].total/3;
if(s1.average>90&&s2.average>90)
{
    strcpy(s1.grade,'A');
    strcpy(s2.grade,'A');
}
else
{
    strcpy(s1.grade,'fail');
    strcpy(s2.grade,'fail');
}

