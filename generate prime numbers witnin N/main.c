#include <stdio.h>
#include <stdlib.h>

int main()
{
    int i,n,c;
    printf("enter the value of n");
    scanf("%d",&n);
    for(i=1;i<=n;i++)
    {
        for(i=1;i<=n;i++)
        c=n%1;
        if(c==0)

    }
}
